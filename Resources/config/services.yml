services:
  sn.request.listener:
    class: SN\RequestBundle\EventListener\RequestListener
    tags:
      - { name: kernel.event_listener, event: kernel.controller }
  sn.exception.listener:
    class: SN\RequestBundle\EventListener\ExceptionListener
    tags:
      - { name: kernel.event_listener, event: kernel.exception }
